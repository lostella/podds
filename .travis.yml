# Documentation: http://docs.travis-ci.com/user/languages/c/
language: c
os:
  - linux
  - osx
compiler:
  - clang
  - gcc
before_install:
  - if [ $TRAVIS_OS_NAME = linux ]; then sudo apt-get install python3.2; else brew install python3; fi
script:
  - make
  - chmod +x podds-test.py
  - ./podds-test.py
notifications:
  email: false
